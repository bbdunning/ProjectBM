{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build & run",
            "type": "shell",
            "command": "cd vsbuild ; if ($?) { ./build.bat }",
            
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}