{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build && run",
            "type": "shell",
            "command": "cd build && make && ./Super_Trash_Bros",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}